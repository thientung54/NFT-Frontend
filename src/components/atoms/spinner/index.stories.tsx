import React from 'react';
import { storiesOf } from '@storybook/react';
import { Spinner } from './index';

storiesOf('Components|atoms/Spinner', module).add('normal', () => <Spinner>Sample test</Spinner>);
