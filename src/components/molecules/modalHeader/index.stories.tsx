import React from 'react';
import { storiesOf } from '@storybook/react';
import { ModalHeader } from './index';

storiesOf('Components|molecules/ModalHeader', module).add('normal', () => <ModalHeader>Sample test</ModalHeader>);
